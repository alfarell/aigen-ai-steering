Ask me to choose exactly one mode: `INITIATE` — inspect the repository and create or populate the AI-native project structure from the provided template; or `WORK EXISTING` — first inspect the current repository, read its existing `AGENTS.md`, `CLAUDE.md`, `.ai/`, documentation, source, tests, and available implementation history, preserve and merge all useful non-conflicting information into the template without overwriting valid project rules or changing production behavior, then use the merged context to complete my requested work.
